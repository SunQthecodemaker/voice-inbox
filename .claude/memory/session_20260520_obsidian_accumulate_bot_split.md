---
name: session_20260520_obsidian_accumulate_bot_split
description: /옵시 슬래시 누적 모드 추가 + voice-inbox intent.md 신규 + 텔레그램 봇 발견·운영 전략 검토 (미결정)
---

# 세션 기록 — 2026-05-20

## 변경 내용

1. **`C:\Users\sunq8\.claude\commands\옵시.md` 누적 모드 추가** (PC1 로컬, NAS 미반영)
   - 트리거: `#그룹명` prefix / 자연어 "~에 기록/추가/넣어줘"
   - 경로: `wiki/{영역}/{그룹}.md`. 없으면 생성, 있으면 `## N+1` append
   - frontmatter 두 종류 (단발/누적). 누적은 `생성일`/`업데이트` 필드
   - 후속 지시: 모음 작성("1~4번으로 회의록"), 항목 삭제·수정, 단발→그룹 이동, 그룹→그룹 이동
   - 운영 룰: 번호 재정렬 안 함(이동·삭제 시 자리 비워둠), 같은 그룹명이 여러 영역에 있으면 한 줄 확인

2. **`z:\web\voice-inbox\intent.md` 신규** (git tracked)
   - 본질: "이 앱은 옵시디언 vault 입력 편의 도구. vault 가 정본, 앱은 인터페이스"
   - 영역 매핑(옵션 A 고정): 병원→치과, 개인업무→개인, 개인생활→기타
   - 그룹 누적 모드 Phase 1 포함
   - 인접 시스템 경계: Claude Code `/옵시` 슬래시 / vault CLAUDE.md / 이 앱

## 결정 사항

- **영역 매핑 옵션 A** (앱 UI 3개 유지, 저장 시 vault 영역으로 변환). 이유: "분류 체계는 항상 vault 기준 통일" 원칙. 사업·학술 메모는 PC `/옵시` 로
- **누적 노트 헤딩 = `## N` 넘버링** (날짜 X). 이유: 후속 지시("1~4번으로 회의록 작성")가 자연스럽게 동작. 1번이 위(시간순)
- **번호 재정렬 안 함**: 이동·삭제로 빠진 자리는 비워둠. 사용자가 부르는 번호 안정성 우선

## 미완료 / 이슈

### 운영 전략 미결정 (가장 중요)

세션 후반에 PC2 텔레그램 봇(`Z:/web/tgbot/`) 존재 + 구조 파악 — **봇이 `claude.exe` CLI 를 subprocess 로 호출하는 게이트웨이**. 즉 봇 메시지가 PC2 의 Claude Code 작업으로 처리됨 → `/옵시` 슬래시·MCP·WebFetch 모두 활용 가능.

사용자 두 핵심 니즈:
1. 단발 메모 (텍스트+보이스) — 간단·빠르게
2. 자연어 가공 지시 (유튜브 정리 등)

니즈 2 는 voice-inbox 로 불가능 → 봇 필수. 단 봇에 "기본은 메모" 디폴트 박으면 봇 본연의 범용 어시스턴트 역할이 묻힘. 사용자가 "봇 여러 개" 가능성 물음.

**검토한 옵션** (미결정):
- 옵션 1: 봇 두 개 분리 — `@SunQ_memo_bot`(신규, 메모 전용) + `@SunQ_assist_bot`(기존, 범용). **내 추천**
- 옵션 2: 한 봇·입력 종류 자동 분기 (음성=메모, 긴 텍스트=어시스턴트)
- 옵션 3: 한 봇·명시 트리거만 (`/메모`, `#그룹` 없으면 항상 어시스턴트)

**voice-inbox Phase 1 자체 보류 검토 중** — 봇이 두 니즈 모두 처리 가능하면 voice-inbox 만들 필요성 낮음. 1~2주 봇으로 실측 후 결정 안 도 거론됨.

### sunq설정 미반영

`옵시.md` 슬래시 정의 PC1 로컬에서만 수정. 다음 `/sunq` 시 NAS 정본(sunq설정.md) 동기화 필요.

## 다음 단계

사용자가 다음 세션에서 운영 전략 결정 후 그에 맞춰 진행:

### 옵션 1 선택 시 (봇 분리)
1. BotFather 에서 새 봇 생성 → 토큰 받기
2. `set-secret.cmd telegram.bot.memo_token "<값>"` 영속화
3. `Z:/web/tgbot/bot.py` 복제해 `bot-memo.py` — 진입점 시스템 프롬프트만 다름 ("모든 메시지는 vault 메모 입력으로 처리. `#그룹` prefix·자연어 누적 지시·명시 작업 지시 우선")
4. `Z:/web/tgbot/install.ps1` 에 새 Task Scheduler 항목 추가
5. 메모 봇에 음성 메시지 STT 단계 추가 (텔레그램 `.ogg` → 텍스트). 어떤 STT 쓸지 결정 필요
6. voice-inbox Phase 1 폐기/보류 결정 → intent.md "결정 기록" 갱신

### 옵션 2·3 선택 시
- 봇 진입점(`bot.py`)의 시스템 프롬프트·분기 로직 수정 단일 작업

### 어느 옵션이든 공통 선행
- `Z:/web/tgbot/bot.py` 코드 확인 — 현재 시스템 프롬프트·`/옵시` 슬래시 호출 가능 여부·음성 메시지 처리 단계 존재 여부

## 환경 의존성

- **`/옵시` 슬래시 누적 모드는 PC1 만**: PC2·PC3 에선 미반영. NAS 정본(sunq설정.md) sync 후에야 다른 PC 도 사용 가능
- **텔레그램 봇** = PC2 상시 가동 가정. `Z:/web/tgbot/` 별도 프로젝트 (이 세션에선 직접 안 봄)
- **vault** = `\\Sunq\sunq\vault\` (Synology Drive sync). `/옵시` 슬래시는 직접 파일 시스템 접근. 봇은 향후 WebDAV 또는 동일 경로
- **voice-inbox 코드 상태** = Phase 1 구현 0 그대로 유지 (변경 없음). next_task 의 7단계 순서는 보류 상태

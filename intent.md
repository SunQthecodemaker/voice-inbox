# voice-inbox intent

> ℹ️ 본 프로젝트의 도메인 의도·페르소나·운영 정책 정본. 코드 룰은 [CLAUDE.md](CLAUDE.md), 절대 규칙은 `Z:/web/CLAUDE.md`.

## 페르소나
SunQ (치과 원장 + 사업체 운영자 + 비전문 개발자). 외출·이동·진료 사이 짬·잠들기 전 등 PC 없는 상황에서 옵시디언 vault 에 메모를 넣고 싶음.

## 핵심 문제
- 떠오르는 생각·아이디어·할 일·회의 안건을 **즉시·간단히** vault 에 박기
- vault 분류·정리에 매번 신경 못 씀 → AI 자동 분류 + 사용자 1탭 확인으로 끝
- 모바일 입력·음성 인식 경유

## 본질 (가장 중요)
**이 앱은 옵시디언 vault 입력 편의 도구.** 자체 데이터 모델·분류 체계·정본 없음. vault 가 정본, 앱은 입력 인터페이스.

→ 영역명·폴더 구조·태그 정책은 **항상 vault 기준**. 앱-vault 사이 불일치가 생기면 vault 쪽에 맞춤. 앱이 vault 구조에 종속.

## 운영 사이클
- **수시 입력**: 외출·이동·진료 사이·잠들기 전. 보이스 또는 모바일 텍스트
- **정리·가공은 PC 에서**: 옵시디언 직접 / Claude Code `/옵시` 슬래시 (이동·모음 작성·번호 정리·archive 등)
- 앱은 "입력만" 담당, 분류 정밀화·관리는 vault 쪽

## 인접 시스템 경계
- **Claude Code `/옵시` 슬래시**: PC 앞에서 같은 vault 에 입력. 누적 노트 구조 (frontmatter + `## N`) 동일 → 같은 `회의안.md` 에 앱·`/옵시` 양쪽에서 추가 가능
- **vault CLAUDE.md** (`z:\sunq\vault\CLAUDE.md`): vault 자체 분류 정책 정본 (영역 5개·폴더 구조)
- **이 앱**: 모바일/보이스 입력 전용 클라이언트. vault 쓰기만, 읽기는 그룹 자동완성용 최소만

## 권한
본인 (sunq818@gmail.com) 전용. WebDAV 인증으로 본인만 vault 쓰기.

---

## 결정 기록

### 2026-05-20 — 영역 매핑 (옵션 A 고정)

앱 UI 영역 3개 → vault 영역 항상 자동 매핑 후 저장:

| 앱 영역 (UI) | vault 영역 (저장) |
|---|---|
| 병원 | 치과 |
| 개인업무 | 개인 |
| 개인생활 | 기타 |

**원칙**: 모든 분류 체계는 vault 기준으로 통일. 앱이 새 vault 폴더 만들지 않음.

**파급**: 앱에서 직접 입력 가능한 vault 영역은 위 3개. `사업`·`학술` 영역 메모는 PC 에서 `/옵시` 로 처리 (모바일에서 일상적으로 발생하는 카테고리 아니라는 판단).

### 2026-05-20 — 그룹 누적 모드 (Phase 1 포함)

입력에 `#그룹명` prefix 또는 "~에 기록해줘" 자연어 패턴이 있으면 → 단발 파일 대신 `wiki/{vault영역}/{그룹}.md` 에 `## N+1` append.

구조·트리거·운영 룰은 Claude Code `/옵시` 슬래시와 동일 (vault 호환성 보장). 양쪽에서 같은 그룹 노트에 자연스럽게 누적됨.
